{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "black this",
            "type": "shell",
            "command": "black ${file} --line-length=120 & isort ${file} --line-length=120"
        }
    ]
}